on: [push]

jobs:
  build_godot_windows:
    runs-on: ubuntu-latest
    name: Build for Windows
    steps:
      - name: Hello world action step
        id: build_win
        uses: bbrieber/godot_ci_image@f2a5986
        with:
          build-config: 'WindowsDesktop'
